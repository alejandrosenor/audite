import { useCallback, useEffect, useState } from "react"; import { useAuth } from "../context/AuthContext"; import { getSocialFeed } from "../services/socialFeed"; import SocialPostCard from "../components/SocialPostCard"; import "./SocialFeed.css";
export default function SocialFeed() { const { user } = useAuth(); const [posts, setPosts] = useState([]); const [loading, setLoading] = useState(true); const load = useCallback(async () => { if (!user?.id) return; setLoading(true); try { setPosts(await getSocialFeed()); } finally { setLoading(false); } }, [user?.id]); useEffect(() => { load(); window.addEventListener("audite:social-feed-changed", load); return () => window.removeEventListener("audite:social-feed-changed", load); }, [load]); if (loading) return <section className="social-feed-page"><p>ACTIVIDAD</p><h1>Cargando el feed...</h1></section>; return <section className="social-feed-page"><header><p>ACTIVIDAD</p><h1>Lo último de tus amigos</h1><span>Escuchas, reseñas y canciones favoritas.</span></header>{posts.length === 0 ? <article className="social-feed-empty"><span>🎧</span><h2>Todavía no hay publicaciones</h2><p>Cuando tú o tus amigos compartáis una valoración, aparecerá aquí.</p></article> : <div className="social-feed-list">{posts.map(p => <SocialPostCard key={p.post_id} post={p} currentUserId={user.id} onChanged={load} />)}</div>}</section> }
